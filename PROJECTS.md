# Projects Index

This repository is organized as a multi-project workspace. Each mini-project lives under `projects/` in its own folder.

Current projects:

- `projects/rekognition_image_label_detection` — Image Label Detection using Amazon Rekognition (Python)

To run the Rekognition project:

```powershell
Set-Location -LiteralPath "..\projects\rekognition_image_label_detection\src"
python main.py
```
